# import numpy as np
# a = np.arange(12).reshape(3,4) #3行4列的矩阵


# print(np.split(a,2,axis=1))   # 矩阵的纵向分割
# print(np.split(a,3,axis=0))   # 矩阵的横向分割


# print(np.array_split(a,3,axis=1))  # array_split可以进行不等的分割

# print(np.vsplit(a,3))  # 在纵向分割 axis=0的分割
# print(np.hsplit(a,2))  # 在纵向分割 axis=1的分割

